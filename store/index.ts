import MetaModule from './modules/meta'

const state =() => ({
});

const getters = {
};

const mutations = {
};

const actions = {
};

export default {
  state,
  mutations,
  actions,
  getters,
  modules: { meta: MetaModule, },
}
